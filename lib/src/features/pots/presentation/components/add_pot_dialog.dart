import 'package:flutter/material.dart';

class AddPotDialog extends StatelessWidget {
  const AddPotDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}