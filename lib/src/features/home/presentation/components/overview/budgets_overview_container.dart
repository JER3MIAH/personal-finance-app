import 'package:flutter/widgets.dart';

class BudgetsOverviewContainer extends StatelessWidget {
  const BudgetsOverviewContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}